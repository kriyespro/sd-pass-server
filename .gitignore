.env
*.pyc
__pycache__/
*.sqlite3
db.sqlite3
.venv/
venv/
.idea/
.DS_Store
staticfiles/
node_modules/
static/css/tailwind.css
media/
data/sites/
data/platform_backups/

# All .txt files (notes, deploy docs, etc.)
*.txt
# Keep Python dependency manifest tracked — remove this line only if you truly
# want requirements.txt ignored (not recommended).
!requirements.txt

# Explicit deny — production secrets must never be committed
env-prod.txt
.env.prod
.env.production
